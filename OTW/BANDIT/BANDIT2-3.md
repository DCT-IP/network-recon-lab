# Bandit 2 -> 3
 - Goal this time was to get password for next level from a file named "--spaces in the file name--"

## commands used
 - ls
 - cat 
 - ./"filename "

## problems encountered 
- obviously cannot use normal cat here, but on using cat "--... " here, it still gave problems

- this was because -- can cause it to trigger options for cat, so the work around is using ./
